{
  "name": "audio-proxy",
  "version": "0.1.0",
  "description": "OpenAI-compatible proxy in front of the IU unified audio endpoint (STT + TTS) with local usage tracking.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "npx kill-port 7716 && bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
